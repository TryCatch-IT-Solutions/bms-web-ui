import Spinner from '@/components/Spinner'

const FullSpinner = () => (
    <div className='h-screen w-full flex items-center justify-center'>
        <Spinner size='40' variant='normal' />
    </div>
)

export default FullSpinner
